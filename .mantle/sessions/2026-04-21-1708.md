---
project: mantle
author: 110059232+chonalchendo@users.noreply.github.com
date: '2026-04-21T17:08:20.303073'
commands_used:
- build
tags:
- type/session-log
---

## Summary
Ran /mantle:build 83 end-to-end. Made `collect-issue-diff-stats` source/test path mapping configurable via a new `diff_paths` field in `.mantle/config.md`, with an `"other"` bucket for unclassified files when the field is explicit.

## What Was Done
- Committed issue-82 closeout (archive, learning, review, state) as a prereq clean-tree commit
- Shaped issue 83 inline — Approach A (additive categorised API), small batch
- Planned 1 story covering all 5 ACs; slice core+cli+tests
- Implemented via opus story-implementer: added `diff_paths` to `_ConfigFrontmatter`, added `DEFAULT_DIFF_PATHS`/`PRIMARY_CATEGORIES`/`load_diff_paths`/`collect_issue_diff_stats_categorised`/`_classify_path`; refactored legacy `collect_issue_diff_stats` to a wrapper; swapped `--shortstat` for `--numstat`; added 10 tests (3 load-paths + 6 categorised + 1 aggregate-filter) plus 2 CLI tests
- Simplifier made one minor import-style fix (`typing.NamedTuple` → direct `NamedTuple`)
- Verification: 5/5 ACs passed; verifier ran a live E2E dbt-style fixture on /tmp

## Decisions Made
- Kept `collect_issue_diff_stats` as a thin wrapper over the new categorised function instead of replacing its signature — zero churn on ~10 existing tests that never write a config.md, and build.md Step 7's skip-heuristic grep stays intact
- `load_diff_paths` catches `FileNotFoundError` from `project.read_config` so tmp_path tests and fresh projects work without a config.md
- `"other"` bucket only emitted when `diff_paths` is explicitly configured — satisfies ac-03 "behaviour matches today"

## What's Next
- /mantle:review for human AC sign-off
- /mantle:retrospective to capture learnings
- Reinstall local `mantle` binary before relying on the new per-category CLI output

## Open Questions
- None