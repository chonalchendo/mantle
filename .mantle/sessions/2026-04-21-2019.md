---
project: mantle
author: 110059232+chonalchendo@users.noreply.github.com
date: '2026-04-21T20:19:10.596285'
commands_used:
- plan-stories
tags:
- type/session-log
---

## Summary
Decomposed issue 84 (model-tier config with measured defaults) into 5 implementable stories covering all 5 ACs and the core/cli/claude-code/tests slice.

## What Was Done
- Story 1: cost-policy.md template shipped from vault-templates/, copied by mantle init
- Story 2: StageModels (frozen Pydantic) + load_model_tier() in core/project.py with preset + overrides precedence and FileNotFoundError fallback to a hardcoded balanced preset
- Story 3: mantle model-tier CLI subcommand — JSON on stdout, summary table on stderr (mirrors mantle introspect-project)
- Story 4: build.md reads tier once, passes STAGE_MODELS.<stage> to every Agent spawn (implement via per-story inner loop, simplify, verify, skill-creation); tier-resolution ordered before skill-creation
- Story 5: Introduce .mantle/telemetry/ via SUBDIRS + hand-authored baseline-<date>.md report comparing an opus-everywhere run to a balanced run

## Decisions Made
- Story 1 skips active migration for existing projects — they rely on Story 2's hardcoded _FALLBACK_STAGE_MODELS when cost-policy.md is absent (avoids mantle install side-effects on a given project; closes shaped-doc open question 1)
- _STAGE_NAMES as single source of truth for StageModels fields + _ModelsConfig.overrides validation (rejects unknown stage keys)
- Closes shaped-doc open question 4: mantle model-tier is a CLI subcommand (not inlined Python) — thin wrapper over core.project.load_model_tier, mirrors mantle introspect-project's stdout/stderr split
- Dollar-cost pricing stays in the baseline markdown, not in code (shaped-doc constraint: no new CLI for measurement); closes shaped open question 3
- Story 4 re-orders Step 3 in build.md so tier-resolution happens BEFORE the skill-creation agent spawn (skill agent uses STAGE_MODELS.plan_stories)

## What's Next
- /mantle:build 84 — stories complete, coverage is 5/5, ready for end-to-end build
- Remaining shaped open question: exact Claude Code Agent model-parameter strings — will surface during Story 4 implementation when first stage model is passed to an Agent spawn

## Open Questions
- Story 5 baseline measurement requires two live /mantle:build runs; implementing agent might bail out on the full measurement and leave a partial report — explicitly flagged in the story
- Whether the implementing agent's session is permitted to run /mantle:build recursively (nested pipeline invocation) — may need to sidestep by running the baseline builds from a fresh session