---
project: mantle
author: 110059232+chonalchendo@users.noreply.github.com
date: '2026-04-16T22:25:26.006304'
commands_used:
- build
tags:
- type/session-log
---

## Summary
Aborted /mantle:build 60 after discovering `mantle where` resolves to the worktree's empty `.mantle/` instead of the primary repo's canonical one, then fixed the underlying bug and cherry-picked onto master.

## What Was Done
- Captured bug via `mantle save-bug` (high severity) in primary `.mantle/bugs/`
- Patched `resolve_mantle_dir` in `src/mantle/core/project.py` with a `_primary_worktree_root` helper that parses `git worktree list --porcelain` (first entry is always primary)
- Added 3 regression tests in `tests/core/test_project.py` using real git repos + worktrees
- Full suite green (1148 passed); lint + type clean
- Committed on worktree branch as `9cd2ed0`, cherry-picked onto master as `f117ef1`

## Decisions Made
- Used `git worktree list --porcelain` over `--git-common-dir` parsing — more explicit and tolerant of separate-git-dir setups
- Only applied fix in local storage path; global-mode returns early, so behavior unchanged there
- Did not touch the pre-existing format drift in issues.py/patterns.py/telemetry.py/test_research.py — not our scope

## What's Next
- Next release will ship the fix; after that, `/mantle:build 60` works from any worktree
- Issue 60 (fix redundant skill loading) remains planned and ready to build — run from primary or post-release
- The captured bug in `.mantle/bugs/` can be archived once the release reinstall confirms the fix end-to-end

## Open Questions
- None