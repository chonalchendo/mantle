---
project: mantle
author: 110059232+chonalchendo@users.noreply.github.com
date: '2026-04-24T18:05:28.973078'
commands_used:
- shape-issue
tags:
- type/session-log
---

## Summary
Shaped issue 92. Originally a narrow read-path fix; widened to Approach D-revised after the user surfaced the requirement that stage attribution must work for every LLM-invoking Mantle command, not just inside /mantle:build.

## What Was Done
- Explored 3 approaches (A keep-old, B replace, C full begin/end markers); derived D-revised (sub-agent read path + universal single-event `mantle stage-begin` primitive) as the only approach that covers standalone commands and gets to "consistently accurate"
- Scope confirmed: instrument all LLM-invoking templates in one pass
- Relocated storage from `.mantle/.stages.jsonl` (hidden root dotfile) to `.mantle/telemetry/stages-<session_id>.jsonl` (per-session, co-located with baseline artefacts)
- Wrote explicit Data-model section (on-disk JSONL shape + `StageMark` vs `StageWindow` Pydantic split)
- Saved shaped doc with --overwrite after the relocation

## Decisions Made
- Single-event marks (no stage-end) — next mark closes prior window; kills orphan-end bookkeeping
- Per-session file keyed by session id in filename — eliminates cross-worktree write races
- Delete dead parent-sidechain clustering + `Marker` / `MarkerWindowWarning` / `_derive_mtime_markers`
- Appetite: medium batch (3-5 days); accepted template-edit volume as the main cost

## What's Next
- Refresh `issues/issue-92-*.md` body to match the D-revised scope + new storage location before plan-stories runs
- Then `/mantle:plan-stories 92`

## Open Questions
- `general-purpose` agentType disambiguation deferred until a non-build caller appears
- Cross-session aggregation order flagged for issue 89, not 92