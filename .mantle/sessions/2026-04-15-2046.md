---
project: mantle
author: 110059232+chonalchendo@users.noreply.github.com
date: '2026-04-15T20:46:24.850837'
commands_used: []
tags:
- type/session-log
---

## Summary
Implemented issue-50 story-01: added `tests/test_staleness_regressions.py` with 8 cross-command side-effect ordering regression tests covering compile lifecycle, CLI ordering, and archive side-effects.

## What Was Done
- New file `tests/test_staleness_regressions.py` with three TestClass groups and a `_make_mantle_fixture` helper
- Compile-lifecycle tests use direct `skills.generate_index_notes()` calls on a vault fixture
- CLI-ordering tests use `subprocess` via `uv run mantle ...` rooted at `tmp_path`
- Archive side-effect tests mix direct `core.archive.archive_issue` calls with CLI subprocess
- Filed inbox item for save-learning silent-success bug (xfailed test)

## Decisions Made
- Used `uv run mantle` (matches `tests/test_package.py`) since `python -m mantle` has no `__main__` module
- Orphan-index cleanup is actually implemented today — that test passes, not xfails (context brief's issue-49 framing was wrong)
- Only one xfail: `test_save_learning_after_archive_fails_clearly` — save_learning does not call find_issue_path

## What's Next
- Review the xfail-documented bug (filed to `.mantle/inbox/`) and decide on save-learning's desired contract
- Run verify/review on issue-50 before approval

## Open Questions
None.