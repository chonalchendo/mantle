---
project: mantle
author: 110059232+chonalchendo@users.noreply.github.com
date: '2026-04-09T16:52:18.044439'
commands_used:
- (story-implementer)
tags:
- type/session-log
---

## Summary
Implemented Story 2 of issue 44: swept the first batch of 7 Claude Code prompt files to substitute hardcoded `.mantle/` Read targets with `$MANTLE_DIR/...` and added the `mantle where` resolve prelude to each.

## What Was Done
- Added tests/test_prompt_sweep.py with two TDD tests (hardcoded-reads + prelude presence)
- Inserted the universal resolve prelude in Step 1 of adopt.md, add-issue.md, add-skill.md, brainstorm.md, bug.md, build.md, challenge.md
- Substituted all Read/Grep tool targets from `.mantle/...` to `$MANTLE_DIR/...` (preserving `mantle init` instruction text, description help strings, mkdir commands, and template example paths)
- Full test suite green (1044 passed), `just check` clean after `just fix`

## Decisions Made
- Kept bug.md description field and the build.md tip sentence ("reads all context it needs from `.mantle/`") as-is — they're help/documentation text, not Read targets
- Left `.mantle/` in `mantle init` instruction strings alone — user-facing messaging stays literal

## What's Next
- Stories 3-5 of issue 44 sweep the remaining prompt files using the identical prelude wording

## Open Questions
- None