---
project: mantle
author: 110059232+chonalchendo@users.noreply.github.com
date: '2026-04-21T18:13:10.023848'
commands_used:
- shape-issue
tags:
- type/session-log
---

## Summary
Shaped issue 84 after a scope-redesign pass. Original umbrella ("programme, not a patch") accreted threads through the conversation until scope hit ~3-4 weeks on a 1-2 week appetite. A first-principles pass narrowed 84 to its dominant lever — per-stage model tier config + measured defaults — and deferred the rest to follow-up issues.

## What Was Done
- Rewrote .mantle/issues/issue-84-cost-and-context-optimisation.md with narrow scope (5 ACs, all directly tied to model tiering + one baseline measurement)
- Saved shaped doc .mantle/shaped/issue-84-model-tier-config-with-measure-shaped.md with chosen approach "Preset + overrides" and full code design
- Saved feedback memory feedback_shaping_scope_creep.md — resist scope creep in shaping; propose splits rather than widening the code design

## Decisions Made
- Drop the umbrella framing. 84 now ships only model tiering. Pruning, review+retro merge, .mantle/ restructure, A/B harness all filed as deferred issues in the issue body
- Config shape: preset + overrides (ergonomic one-knob common case; overrides as escape hatch) rather than flat per-stage
- Measurement: reuse existing core/telemetry.py JSONL parser — no new CLI; write baseline report by hand into new .mantle/telemetry/ folder
- Dollar cost and wall-clock seconds are the metrics, not token counts — what actually hurts the user

## What's Next
- /mantle:plan-stories to decompose 84 into implementable stories
- Optionally /mantle:add-issue to file the deferred items (review+retro merge; prompt pruning; .mantle/ restructure)

## Open Questions
- cost-policy.md migration mechanism for existing projects (new `mantle install` behaviour?)
- Exact model IDs Claude Code's Agent `model:` parameter accepts
- Where per-model dollar prices live (hardcoded table in baseline template, or config)
- mantle model-tier as its own CLI subcommand vs inlined Python