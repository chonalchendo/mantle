---
project: mantle
author: 110059232+chonalchendo@users.noreply.github.com
date: '2026-04-07T16:31:06.683828'
commands_used: []
tags:
- type/session-log
---

## Summary
Implemented issue 38, story 1: fixed two bugs in `simplify.collect_issue_files()` — the grep pattern was too loose (matching issue-3 when issue-30 was present), and no-commits raised an exception instead of returning empty.

## What Was Done
- Removed `NoCommitsFoundError` exception class from core/simplify.py
- Fixed grep pattern from `issue-{N}` to `(issue-{N})` to use conventional commit scope delimiters
- Added zero-padded fallback search `(issue-0N)` for single-digit issues
- Changed no-commits path to return `()` instead of raising
- Updated CLI to check empty tuple and print message with exit 0 (no SystemExit)
- Added file count summary line to CLI happy path output
- Replaced `test_no_commits_raises` with `test_no_commits_returns_empty`
- Added `test_excludes_partial_number_match` regression test
- Added `test_finds_zero_padded_commits` test
- Updated CLI test from `test_no_commits_exits` to `test_no_commits_prints_message`

## Decisions Made
- Nested `_grep_commits` helper defined inside `collect_issue_files` to avoid exposing an internal-only function in the module's public API

## What's Next
- Run /mantle:build pipeline for issue 38

## Open Questions
- None