---
project: mantle
author: 110059232+chonalchendo@users.noreply.github.com
date: '2026-04-09T09:38:07.203150'
commands_used:
- add-issue
tags:
- type/session-log
---

## Summary
Prioritised 5 CLI ergonomics issues from work usage, then captured issue-43 (new) for the first P0 fix.

## What Was Done
- Critical-thinking pass on 5 reported friction points; root-caused to defaults + prompt-level path resolution, not a CLI rewrite
- Split work into 2 issues: Issue A (path resolution fix) + Issue B (init flag ergonomics, depends on A)
- Decided Shape A (explicit --global flag only, no user-level default)
- Ran /mantle:add-issue for Issue A → saved as issue-43 "Fix hardcoded .mantle/ path reads in Claude Code prompts"

## Decisions Made
- No CLI rewrite — targeted fixes instead. Diagnosis was defaults + one resolution bug, not command surface
- Two-issue split so the bug fix ships independently of the ergonomics redesign
- Shape A (flag only) chosen over Shape C (flag + user config) to avoid "same command behaves differently on different machines"
- Codebase-analyst truncation (originally issue #5) dropped — user confirmed it worked on retry
- README phantom --global flag folded into Issue B (to be created later)

## What's Next
- Run /mantle:build 43 to execute the path resolution fix
- After 43 ships, run /mantle:add-issue for Issue B (init --global, init --vault, README fix)

## Open Questions
- None