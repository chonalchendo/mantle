---
project: mantle
author: 110059232+chonalchendo@users.noreply.github.com
date: '2026-04-09T16:30:57.621953'
commands_used:
- shape-issue
tags:
- type/session-log
---

## Summary
Shaped issue 44 (fix hardcoded .mantle/ path reads in Claude Code prompts). Chose Approach A: a thin `mantle where` CLI command plus a sweep of 22 prompt files batched per the issue 43 learning.

## What Was Done
- Loaded context: issue 44 description, product/system design, issue 43 archived learning, existing `project.resolve_mantle_dir()` resolver, current cli/storage.py and cli/main.py patterns
- Explored three approaches: (A) `mantle where` CLI + prompt sweep, (B) CLI owns all file reads via ~15 read subcommands, (C) symlink on storage-mode transition
- Confirmed with user that global mode requires literally nothing in the project directory — ruled out Approach C
- Drafted code design with 5-story decomposition (1 CLI command + 3 sweep batches of ~7 files + 1 integration test)
- Saved shaped issue via `mantle save-shaped-issue`

## Decisions Made
- Approach A chosen: matches existing prompt/CLI contract, reuses existing core resolver, preserves Read tool affordances, follows "one command, one job" principle
- CLI name: `mantle where` (short, memorable, no flags)
- Implementation lives in `cli/storage.py` as the read-side counterpart to `run_storage`/`run_migrate_storage`
- Plain `print()` not `console.print()` to keep Bash captures clean
- Prompt sweep split into 3 batches of ~7 files per issue 43 learning ("split N>7 file stories")

## What's Next
- /mantle:plan-stories 44 to decompose into the 5 sketched stories with TDD specs

## Open Questions
- None