---
date: 2026-04-21
issue_measured: pending
session_before: pending
session_after: pending
status: skeleton
tags:
  - type/telemetry
---

## Status

**Skeleton — pending live measurement runs.**

Companion file: `baseline-2026-04-21.json` — same data in machine-readable
form for PR-diff review. Keep both files in sync when filling in live numbers.

This report was authored during issue 84's build pipeline, which cannot run
two nested `/mantle:build` invocations inside its own session. Filling in
the numbers below requires two real `/mantle:build <NN>` runs executed
from a fresh session:

1. **Before (Opus everywhere):** a run with `models.preset: quality` plus
   overrides to force every stage to `opus`, approximating the pre-issue-84
   default behaviour.
2. **After (balanced):** a run with `models.preset: balanced` and no
   overrides.

Record both Claude Code session UUIDs, parse each JSONL via
`mantle.core.telemetry` (`read_session` → `group_stories` → `summarise`),
and apply the pricing table below to convert tokens to dollar cost.

## Method

Two `/mantle:build <NN>` runs on the same issue (or equivalent issues of
similar shape) under two configurations:

- **Before** — `preset: quality` with every stage forced to `opus`.
- **After** — `preset: balanced` (no overrides).

Session JSONLs parsed via `mantle.core.telemetry`. Dollar cost computed
from the published Claude prices in the pricing table below, multiplied
by the per-model token counts reported by the telemetry module.

## Prices used

_Fill in with the current published Anthropic rates at measurement time._

| Model  | Input $/MT | Output $/MT | Cache read $/MT | Cache write $/MT |
|--------|-----------:|------------:|----------------:|-----------------:|
| opus   | <fill>     | <fill>      | <fill>          | <fill>           |
| sonnet | <fill>     | <fill>      | <fill>          | <fill>           |
| haiku  | <fill>     | <fill>      | <fill>          | <fill>           |

## Results

| Stage           | Before model | After model | Before $ | After $ | Before seconds | After seconds |
|-----------------|--------------|-------------|---------:|--------:|---------------:|--------------:|
| shape           | opus         | opus        | <fill>   | <fill>  | <fill>         | <fill>        |
| plan_stories    | opus         | sonnet      | <fill>   | <fill>  | <fill>         | <fill>        |
| implement       | opus         | sonnet      | <fill>   | <fill>  | <fill>         | <fill>        |
| simplify        | opus         | sonnet      | <fill>   | <fill>  | <fill>         | <fill>        |
| verify          | opus         | sonnet      | <fill>   | <fill>  | <fill>         | <fill>        |
| review          | opus         | haiku       | <fill>   | <fill>  | <fill>         | <fill>        |
| retrospective   | opus         | haiku       | <fill>   | <fill>  | <fill>         | <fill>        |
| **Total**       |              |             | **<x>**  | **<y>** | **<a>**        | **<b>**       |

## Interpretation

_Fill in 1–2 paragraphs after the measurement runs complete. Call out
any stage where the after run was slower than the before run (expected
on stages where haiku/sonnet needs more retries)._
